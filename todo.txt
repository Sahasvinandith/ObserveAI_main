right now detection system models from our 2 cameras are changing the globalpersons local_user_id and names without leaving any trace of which fead provided which user name , 


@DetectionSystem.py#L454-458 also in here i see you  have hard coded these values instead of getting them from settings. implement that 

Sharpness and Blur

Compute Laplacian variance or CPBD on the cropped face_img for focus quality. Threshold low scores (<100-500, tune on data) to discard blurry/motion-affected detections common in CCTV.

Example: cv2.Laplacian(gray_face, cv2.CV_64F).var() via OpenCV.​

