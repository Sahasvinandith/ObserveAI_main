This directory is populated at Docker build time by the Dockerfile COPY instruction.
The build context is the project root, so all source files are copied from there.
Do not place files here manually — they will be overwritten during builds.
